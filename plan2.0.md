小窗口指搜索结果点进去的窗口，主窗口指搜索界面的窗口

# 1.优化搜索结果显示方式
目前的搜索结果显示逻辑是：搜索结果仅显示表名，然后在旁边标注命中了几个值。例如在 A 表当中命中了 6 个，但我目前不知道这 6 个具体分别是哪些内容，如果想知道的话，必须点进去才能看到。

我要求改为：每行显示一个标注内容。例如 A 表有 6 个命中内容，那么就一行显示一个。第一行显示 A 表的第一个命中内容，第二行显示 A 表的第二个命中内容，以此类推。
# 2.小窗口最上面的标题旁边增加一个复制按钮，并且支持标题选中复制
# 3.小窗口支持窗口的大小拉伸，支持手动调整小窗口的大小
# 4.修改界面显示布局：
![[Pasted image 20260228155021.png]]
需要参考的地方参考这张图的结果，重点关注筛选栏的设计，新版界面改为全部、表名、字段名、备注、数据值这几种。例如，搜索后，点击表名，则只显示搜索结果里为表名的搜索结果。依然默认不搜索数据值，按回车才搜索数据值