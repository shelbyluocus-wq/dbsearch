# 2026-04-25

## 同步中心 UI 样式调整
修改文件：`e:\project\dbsearch\src\styles.css`

### 完成的修改
1. **字体更细**：侧边栏和内容区 font-weight 从 800/850/700 降低到 400-600
2. **右侧大底板删除**：移除 `.sw-content` / `.dmw-content` 的毛玻璃底板（background→transparent, 去掉 border/box-shadow/backdrop-filter）
3. **左侧颜色调整**：侧边栏底板改为 `rgb(60, 73, 83)`，字体改为 `rgb(246, 248, 250)` 更白
4. 修改覆盖了三层CSS定义（基础层 11291-11322、中间层 11743-12333、最终覆盖层 12549-12804）

### 修改要点
- CSS 有多层覆盖结构，同属性需在多层同步修改
- `.sync-center-sidebar-shell` 是最终侧边栏样式，不在 `.sync-center-file-body` 内
- `--sc-text-soft` CSS变量被多处引用，改它会全局生效

## 同步中心 UI 二次调整
修改文件：`src/App.vue` + `src/styles.css`

### 完成的修改
1. **删除"项目与任务"标题**：移除 `sync-center-sidebar-caption` div（App.vue 行7977）
2. **转表和数据库同步标题对齐**：给数据库同步的 section-head 添加 `sync-center-sidebar-icon`（⇄），替换原来的 status-dot，统一与转表的结构
3. **左侧滚动支持**：`.sync-center-sidebar-shell` 的 `overflow: hidden` 改为 `overflow-y: auto`，并添加自定义滚动条样式（4px宽、半透明）

## 同步中心 UI 三次调整
修改文件：`src/App.vue` + `src/styles.css` + `src/DbMigrationWorkspace.vue`

### 完成的修改
1. **隐藏滚动条**：将自定义滚动条样式改为 `display: none` + `scrollbar-width: none`，滚动功能保留但不可见
2. **转表空状态统一**：将 `DbMigrationWorkspace.vue` 中的 `dmw-empty-state` 卡片风格改为 `sw-empty` 居中风格，与数据库同步空状态一致；同时在 styles.css 中为 `.sync-center-root .sw-empty-title/desc` 添加白色文字覆盖
3. **右上角按钮修复**：添加 `padding: 0`、`line-height: 32px`、`cursor: pointer`、`pointer-events: auto` 确保图标居中且可点击

## 同步中心 UI 四次调整
修改文件：`src/App.vue` + `src/styles.css` + `src/DbMigrationWorkspace.vue`

### 完成的修改
1. **删除右上角两个按钮**：移除 `sync-center-top-actions` 整个 div（含"新增"和"设置"按钮）
2. **模板右键删除菜单**：
   - 添加 `syncCenterCtxMenu` 状态变量，`openSyncCenterCtxMenu`/`closeSyncCenterCtxMenu`/`deleteSyncCenterProfile` 三个函数
   - 转表模板按钮加 `@contextmenu.prevent="openSyncCenterCtxMenu($event, profile.id, 'database')"`
   - 数据库同步模板按钮加 `@contextmenu.prevent="openSyncCenterCtxMenu($event, profile.id, 'file')"`
   - 右键菜单模板用 `sync-center-ctx-menu` 类，深色半透明风格，点击空白关闭
   - DbMigrationWorkspace 新增 `removeProfile` 方法并 expose

## 同步中心 UI 五次调整
修改文件：`src/DbMigrationWorkspace.vue` + `src/styles.css`

### 完成的修改
1. **删除转表无模板时的头部3行**：移除 `sw-content-header`（标题、副标题、状态行），无模板时只显示空状态
2. **空状态垂直居中**：`.sync-center-root .sw-empty` 添加 `flex: 1; margin: auto 0;` 实现在内容区域水平垂直居中，跟随窗口大小实时调整

## 同步中心 UI 六次调整
修改文件：`src/App.vue` + `src/styles.css`

### 完成的修改
1. **红绿灯上移**：`.sync-center-sidebar-chrome` margin-bottom 18→10，height 34→28
2. **红绿灯缩小**：`.traffic-btn` width/height 20→14，lights gap 10→8
3. **删除搜索功能**：移除搜索框模板（label.sync-center-sidebar-search）

## 同步中心 UI 七次调整
修改文件：`src/styles.css`

### 完成的修改
1. **字体更细**：所有侧边栏文字 font-weight 从 650/700/800/850 统一降到 400
2. **右侧大底板删除**：`.sync-center-view` 设为 `display: none`
3. **字体色值统一为 rgb(246, 248, 250)**：
   - CSS 变量 `--sc-text`、`--sc-text-soft`、`--sc-text-dim` 全部设为此值
   - 侧边栏标题(section-head)、模板项(sw-sidebar-item)、空状态、时间文字、图标、chevron箭头、搜索框等全部改为此颜色

## 同步中心 UI 八次调整
修改文件：`src/styles.css` + `src/App.vue`

### 完成的修改
1. **修正右侧底板删除**：上次错误地将 `.sync-center-view` 设为 `display: none`，现修正为只移除内容区底板效果：
   - `.sync-center-file-body .sw-content` 和 `.dmw-root--embedded .dmw-content` 的 background→transparent, border→none, box-shadow→none, backdrop-filter→none
   - 修改覆盖了所有4层CSS定义（基础层、中间层、最终覆盖层各两处）
2. **删除右上角两个按钮**：移除 `sync-center-top-actions` div（含"新增+"和"设置⚙"按钮）

## 同步中心 UI 九次调整 - 名称互换 & 弹窗删除
修改文件：`src/App.vue` + `src/DbMigrationWorkspace.vue` + `src/styles.css` + `src/syncCenterUi.test.js`

### 完成的修改
1. **互换转表和数据库同步的名称与图标**：左侧 sidebar 上方 section（database 模式）改为"数据库同步"+⇄+is-database-sync，下方 section（file 模式）改为"转表"+⌘+is-transfer；日志标题同步互换
2. **删除转表添加模板弹窗**：移除 `sc-add-modal-backdrop` 全部 HTML、`dbAddModalVisible`/`dbAddForm`/`openDbAddModal`/`closeDbAddModal`/`confirmDbAddModal` 全部状态和函数、`sc-add-modal-*`/`sc-add-field-*`/`sc-add-btn-*` 全部 CSS
3. **修改添加模板逻辑**：`addDbMigrationProfileFromSyncCenter()` 现在直接调用 `dbMigrationWorkspaceRef.value?.addProfile?.()` 创建空模板并在右侧编辑
4. **修复右键删除不生效**：`DbMigrationWorkspace` 的 `defineExpose` 缺少 `removeProfile` 方法，已添加并暴露

## 同步中心 UI 十次调整 - 无模板时隐藏header & 空状态统一居中
修改文件：`src/DbMigrationWorkspace.vue` + `src/styles.css`

### 完成的修改
1. **无模板时删除3行header**：移除 `v-else` 分支中的 `sw-content-header`（"数据库迁移工作台"标题、副标题、状态行）
2. **空状态样式统一**：将数据库同步的 `dmw-empty-state` 卡片风格改为 `sw-empty` 居中风格，与转表一致
3. **空状态居中CSS**：添加 `.dmw-root--embedded .sw-empty` 居中规则（flex:1 + justify-content:center + align-items:center），与 `.sync-center-root .sw-empty` 统一

## 同步中心 UI 十一次调整 - 流程式卡片重构
修改文件：`src/DbMigrationWorkspace.vue` + `src/styles.css` + `src/App.vue`

### 完成的修改
1. **流程式卡片设计**：数据库同步主区改为两步式卡片——Step 1 连接配置 + Step 2 选择数据库
2. **Step tabs 导航**：添加 `dmw-steps` 步骤指示器，点击可在两步之间切换
3. **Step 1 连接配置卡片**：内联编辑模板名称、主机、端口、账号、密码，底部"下一步"按钮（测试连接后跳转）+ "导入 JSON" + "删除模板"
4. **Step 2 选择数据库卡片**：运行按钮 + 打开备份目录 + 源库/目标库下拉框
5. **自动步骤跳转**：watch `activeProfileConnectionReady` 和 `activeProfileId`，未连接时自动切到 Step 1
6. **精简设置浮层**：移除模板管理编辑区，只保留快捷键设置
7. **`importDbConfigFile` 改为在主区工作**：不再依赖设置面板，直接修改 activeProfile
8. **`saveSettings` 简化**：只保存快捷键，不再处理模板 draft
9. **删除冗余元素**：dmw-section-primary、dmw-incomplete-card、dmw-selection-preview-grid、dmw-inline-note、dmw-path-card
10. **统一术语**：转表"配置路径"改为"连接配置"
11. **新增 CSS**：dmw-steps、dmw-step-tab、dmw-step-card 等流程式卡片样式
