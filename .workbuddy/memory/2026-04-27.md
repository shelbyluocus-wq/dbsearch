# 2026-04-27 工作日志

## 同步中心 UI 可读性优化
优化了同步中心界面中多个看不清的元素（用户截图标注）：

### 右侧日志面板
- **时间戳** `.sw-log-entry-time`：`#86868B` → `rgba(166,182,206,0.88)` + 加粗，从暗灰改为亮蓝灰
- **运行中状态**：`#FF9500` → `#FFB340`（更亮的橙黄）+ 650 字重 + 橙色光晕
- **完成状态**：`#34C759` → `#5DECA3`（更亮的薄荷绿）+ 650 字重 + 绿色光晕
- **失败状态** → `#FF6B60` + 光晕
- **命令行文字** `.sw-log-entry-cmd`：`#007AFF` → `#5EB4FF`（更亮的蓝色）
- **黑底代码块** `.sw-log-entry-detail`：背景加深为 `rgba(6,12,22,0.92)` + 文字提亮为 `#F0F2F5` + 蓝色边框

### 中间区域
- **Banner 提示**：为 tone-running/success/neutral 全部添加了同步中心专用高对比度样式
  - running: 金黄色字 `#FFD280` + 暖棕渐变背景
  - success: 薄荷绿字 `#8FF0B4` + 深绿渐变背景
  - neutral: 使用 `--sc-text-soft` + 冷灰渐变背景
- **步骤指示器** dmw-step-tab: active 态加白色+蓝色光晕，done 态用亮蓝灰

## 第二轮优化（用户反馈后）

### 1. 数据库选择框文字截断修复
- **根因**：`.sync-center-root .sw-config-form` 容器的 `overflow: hidden` 裁剪了 select 框内文字
- **修复**：将 `overflow: hidden` → `overflow: visible`
- **额外**：为 sync-center 下 dmw-select 增加 `padding-left: 16px`、`flex: 1`、`min-width: 0`，确保 grid 两列布局下文字完整显示

### 2. 日志面板文字进一步加深（用户反馈仍然太浅）
- **时间戳**：`rgba(166,182,206,0.88)` → `rgba(198,212,232,0.95)` + 字重 500→600 + 新增 text-shadow
- **运行中状态**：`#FFB340` → `#FFCC66`（更亮金黄）+ 字重 650→700 + 双层光晕
- **完成状态**：`#5DECA3` → `#6FF5B0`（更亮薄荷绿）+ 字重 650→700 + 双层光晕
- **错误状态**：`#FF6B60` → `#FF7B70` + 字重 650→700 + 双层光晕
- **命令行**：`#5EB4FF` → `#70C0FF` + 字重 500→600 + text-shadow
- **消息文字**：`var(--sc-text-soft)` → `rgba(230,238,250,0.92)` + text-shadow
- **日志条目卡片**：新增 `.sync-center-root .sw-log-entry` 深色半透明背景 `rgba(30,45,68,0.42)` + 毛玻璃效果
- **idle 状态**：新增显式样式 `rgba(210,222,240,0.85)` + 550 字重

### 3. 数据库选择框文字垂直截断修复
- **根因**：基础 `.dmw-select` 设置了 `line-height: 1`（第10893行），同步中心覆盖时未重置，导致 select 框内文字（edota_17cfg 等）上下被裁剪
- **修复**：在两处同步中心 `.dmw-select` 覆盖规则中均添加 `line-height: 1.5` + 显式 `font-size: 14px`
  - 主样式区（~第13520行）
  - 响应式 media query 区（~第13968行）

### 4. 主搜索排序逻辑优化
**用户反馈**：搜索 "heroprop" 时第一个结果是 guideheroshowprop，精确匹配的 heroprop 反而排后面

**根因（双重问题）**：
1. **Bug**：sortedSearchTableResults 在 rankTableSearchCandidates() 排好后又调用 sortTableItems() 按字母表重排
2. **算法缺失**：缺少"完全相等"最高优先级 Tier

**修复**：
- panelChrome.js rankTableSearchCandidates(): 新增 Tier 0 完全相等匹配(50000分)；优化 Tier 2 中间连续匹配的位置加权
- App.vue sortedSearchTableResults: 移除搜索模式下的 sortTableItems() 调用
- 新增测试用例验证 heroprop 场景（33/33 通过）
