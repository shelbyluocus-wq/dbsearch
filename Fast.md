# 这个项目的快捷键说明

这份是我直接对着当前项目源码整理的，尽量只写“代码里真的有处理”的键，不拿系统默认按键凑数。

先说两个小前提：

- 下面提到的 `Ctrl`，在 macOS 上很多场景基本可以理解成 `Cmd`，因为源码里不少地方同时兼容了 `ctrlKey` 和 `metaKey`。
- 我把内容分成两类：一类是“真正的快捷键”，一类是“进入某个界面以后很好用的操作键”。

## 1. 可以改的快捷键

这些键基本都能在设置里改，默认值如下。

| 默认键位 | 干什么用 | 大白话解释 |
| --- | --- | --- |
| `Ctrl+Shift+F` | 打开/隐藏搜索面板 | 这是主快捷键。你不想用鼠标点小宠物时，直接按它就能把搜索面板叫出来；如果面板已经开着，再按一次会收起来。 |
| `F9` | 插入今天日期 | 光标在输入框里时，按一下会直接塞入当天日期，格式是 `YYYYMMDD`。填日期字段时很省事。 |
| `Shift+S` | 打开/隐藏同步工作台 | 这是“一键同步工作台”的呼出键。想开同步窗口就按一下，已经开着就再按一下收起来。 |
| `Ctrl+E` | 导出当前表 | 你现在正在看的这张表，直接导出成 Excel。 |
| `Ctrl+Shift+E` | 批量导出 | 一次挑多张表一起导出，不用一张一张来。 |
| `P` | 切换面板置顶 | 这个比较猛，是单键。按一下置顶，再按一下取消置顶。为了防误触，焦点在输入框里时不会触发。 |
| `Ctrl+Alt+Left` | 切到上一个数据库模板 | 数据库模板配了好几套时，往前翻一个。 |
| `Ctrl+Alt+Right` | 切到下一个数据库模板 | 跟上面相反，往后翻一个。 |

补一句：

- 模板切换快捷键虽然能改，但源码里专门拦了冲突，不能跟 `Ctrl+P`、`Ctrl+O`、`Ctrl+[`、`Ctrl+]` 这些内置键撞车。

## 2. 搜索面板里最常用的键

### 搜索框本体

| 键位 | 干什么用 | 大白话解释 |
| --- | --- | --- |
| `Enter` | 开始搜索 | 关键词输完直接回车，不用点“搜索”。 |
| `/` | 进入 Slash 模式 | 相当于先圈定要搜哪些表，再搜，范围更准也更快。 |
| `Backspace` | 删除最后一个已选表 | 这个只在搜索框已经空了的时候生效。你前面选过表范围，想撤掉最后一个，直接退格。 |

### 不打开表窗口时的键盘导航

这组键很多人容易漏掉，但其实很好用。

| 键位 | 干什么用 | 大白话解释 |
| --- | --- | --- |
| `Ctrl+Left` / `Ctrl+Right` | 在左侧分类和右侧结果区之间切换焦点 | 不用鼠标来回点区域，直接把“键盘操作权”切过去。 |
| `Ctrl+Up` / `Ctrl+Down` | 在当前焦点区域里上下移动 | 焦点在左侧时切分类，焦点在右侧时切结果。 |
| `Enter` | 打开当前高亮结果 | 你用方向键选到哪条，回车就开哪条。 |
| `Esc` | 关闭 Slash 模式 | 只要现在开着 Slash 选表面板，按它就收。 |

## 3. Slash 模式怎么用

你在搜索框里按 `/` 以后，会进一个“限定搜索范围”的小模式。这里也有一套键。

| 键位 | 干什么用 | 大白话解释 |
| --- | --- | --- |
| `ArrowDown` | 选下一个候选项 | 往下走。 |
| `ArrowUp` | 选上一个候选项 | 往上走。 |
| `Enter` | 确认选择 | 选中当前这张表，或者确认当前候选。 |
| `Esc` | 退出 Slash 模式 | 不想选了，直接关。 |
| `Backspace` | 删 Slash 查询词，删空后退出 | 有字就删字；没字了就直接退回普通搜索。 |
| 直接输入字符 | 过滤候选表 | 就像在一个小搜索框里继续打字，越打越精准。 |

## 4. 主面板 / 表窗口里的快捷键

这个部分是项目里键位最密的一块。有些键只在表窗口里生效，有些在主面板里也能直接用，我一起放这了。

| 键位 | 干什么用 | 大白话解释 |
| --- | --- | --- |
| `Ctrl+P` | 打开“表命令面板” | 不管你现在在主面板还是已经打开了表，都能直接呼出一个“按表名快速跳转”的输入框。 |
| `Ctrl+O` | 打开表内查找 | 这个是“在当前表里找内容”，不是全库搜索。 |
| `Ctrl+[` | 切到当前搜索结果里的上一张表 | 注意，它不是简单切上一个 tab，而是按当前搜索结果的表顺序往前跳。 |
| `Ctrl+]` | 切到当前搜索结果里的下一张表 | 跟上面相反，按结果顺序往后跳。 |
| `Ctrl+Tab` | 切到下一个已打开标签 | 这个才是切 tab。 |
| `Ctrl+Shift+Tab` | 切到上一个已打开标签 | 往回切 tab。 |
| `Tab` | 切换“只看命中” / “完整表视图” | 一下看命中行，一下看整张表，两种视图来回切。 |
| `W` | 全屏 / 退出全屏 | 表窗口放大看，更适合盯数据。 |
| `Ctrl+W` | 关闭当前表 | 只关当前这一张。 |
| `Alt+W` | 关闭全部表 | 打开太多表了，一把清掉。 |
| `Q` | 跳到上一条命中行 | 看搜索命中的上下文时很顺手。 |
| `E` | 跳到下一条命中行 | 跟 `Q` 配对使用。 |
| `` ` `` | 进入或退出编辑模式 | 想改表数据时，用这个切进编辑模式。 |
| `Ctrl+Left` / `Ctrl+Right` | 横向滚动表格 | 字段很多、表太宽时特别有用。 |
| `Esc` | 关闭当前这层东西 | 比如关表内查找、关弹窗、关表窗口，谁在最上层就先关谁。 |

这里有两个很容易搞混的点：

- `Ctrl+[` / `Ctrl+]` 是“按当前搜索结果切表”。
- 真正切已经打开的 tab，要用 `Ctrl+Tab` 和 `Ctrl+Shift+Tab`。

## 5. 表命令面板里的键

按 `Ctrl+P` 打开以后，这个小面板也支持纯键盘操作。

| 键位 | 干什么用 | 大白话解释 |
| --- | --- | --- |
| `ArrowDown` | 选下一个候选表 | 往下挑。 |
| `ArrowUp` | 选上一个候选表 | 往上挑。 |
| `Enter` | 打开当前选中的表 | 不用鼠标点，直接开。 |
| `Esc` | 关闭命令面板 | 退出。 |
| `/` | 进入 Slash 选择模式 | 面板刚打开、输入还是空的时候，按它能切到 Slash 模式。 |
| `Backspace` | 退出 Slash 模式 | 也是在查询为空时生效。 |

## 6. 表内查找的键

按 `Ctrl+O` 打开之后，可以这样用：

| 键位 | 干什么用 | 大白话解释 |
| --- | --- | --- |
| `Enter` | 跳到下一处匹配 | 一处一处往下找。 |
| `Shift+Enter` | 跳到上一处匹配 | 往回找。 |
| `Esc` | 关闭表内查找条 | 查完就收起来。 |

## 7. 编辑相关的键

### 单元格详情编辑器

有时候你点开的是单元格详情/大文本编辑器，这里也有快捷键。

| 键位 | 干什么用 | 大白话解释 |
| --- | --- | --- |
| `Ctrl+S` | 保存当前单元格修改 | 改完这一个值，直接保存回表格里的暂存修改。 |
| `Esc` | 关闭编辑器 | 如果有未保存内容，会先弹一个“要不要放弃修改”的确认框。 |

### 表格内联编辑

| 键位 | 干什么用 | 大白话解释 |
| --- | --- | --- |
| `Enter` | 确认当前单元格编辑 | 改完这个格子，回车确认。 |
| `Esc` | 取消当前单元格编辑 | 这格不改了，撤回。 |
| `Tab` | 确认并跳到下一列继续改 | 很适合一行里连续改多个字段。这个主要用在已有数据行上。 |

### 新增行编辑

| 键位 | 干什么用 | 大白话解释 |
| --- | --- | --- |
| `Enter` | 确认当前输入 | 把这个新增单元格的内容先记下来。 |
| `Esc` | 取消当前输入 | 不想改了就退。 |

### 进入编辑模式的日期验证弹窗

| 键位 | 干什么用 | 大白话解释 |
| --- | --- | --- |
| `Enter` | 提交当天日期 | 进入编辑模式前要输当天日期，输完直接回车确认。 |

## 8. `Esc` 在这个项目里基本就是“收一层”

这个键项目里用得非常多，可以理解成统一的“先把最上面那层收掉”。

常见场景包括：

- 关 Slash 模式
- 关表命令面板
- 关表内查找
- 关单元格编辑器
- 关放弃修改确认框
- 关导出弹窗
- 关结果放大弹窗
- 关表窗口
- 关设置窗口
- 在桌面端里，如果前面这些都没开，它还会把主面板隐藏起来

## 9. 最后补一个不算纯键盘快捷键，但也挺像“快捷操作”

源码里还有一个很实用的小动作：

- 按住 `Ctrl` 再滚鼠标滚轮，可以调界面缩放。

这个不算标准意义上的“快捷键”，但确实是代码里专门做过的，所以顺手写上。

## 10. 如果你想改这些快捷键，怎么改

项目里录快捷键的方式也挺直接：

- 点进设置里的快捷键输入框
- 直接按你想要的组合
- 如果想清空，就按 `Backspace` 或 `Delete`

其中同步工作台的快捷键是在“同步工作台设置”里单独改，不跟主设置放在一起。

---

如果只记最常用的几个，优先记这几个就够了：

- `Ctrl+Shift+F`：呼出搜索面板
- `F9`：填今天日期
- `Ctrl+P`：快速开表
- `Ctrl+O`：在当前表里查
- `Tab`：切换命中视图和完整视图
- `` ` ``：进编辑模式
- `Ctrl+E`：导出当前表
- `Shift+S`：打开同步工作台
