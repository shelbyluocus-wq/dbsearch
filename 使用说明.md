# 鹰捷（DB Scout）使用说明

一款桌面小工具，用来快速搜索 MySQL 数据库里的表名、字段名、备注和数据，还自带一个可爱的像素风小鹰宠物浮窗。

---

## 快速上手

1. 打开软件，桌面上会出现一只小鹰浮窗
2. 右键小鹰 → 点「设置」，填好数据库连接信息（地址、端口、用户名、密码、数据库名），保存
3. 点「连接」按钮，连上数据库
4. 在搜索框输入关键词，回车开始搜索

---

## 主要功能

### 1. 数据库搜索

在搜索框输入关键词就能搜，支持三种搜索范围：

| 模式 | 说明 |
|------|------|
| 全部 | 同时搜表名、字段名、备注和数据（默认） |
| 仅元数据 | 只搜表名、字段名、备注，不搜数据内容 |
| 仅数据 | 只搜数据内容，不搜表名字段名 |

搜索结果分四个标签页展示：
- **表名** — 匹配到的表
- **字段名** — 匹配到的字段
- **备注** — 匹配到的表/字段备注
- **数据值** — 匹配到的数据行

点标签页标题可以展开查看该类全部结果。

**指定搜索范围：** 在搜索框输入 `/`，会弹出表选择列表，选中后只搜这几张表，搜索更快更精准。

**搜索历史：** 点搜索框旁边的「历史」按钮，可以查看和重用之前的搜索记录。

### 2. 查看表数据

点击搜索结果中的表名，会打开表数据查看窗口：

- **标签页浏览** — 最多同时打开 8 张表，点标签切换
- **两种视图** — 按 `Tab` 键切换：
  - 「只看命中」：只显示包含搜索关键词的行
  - 「正常页面」：显示表的全部数据，支持翻页
- **表结构** — 页面上方显示表名、备注、所有字段的名称和类型
- **全屏模式** — 按 `W` 键可以全屏查看表数据
- **表内搜索** — 按 `Ctrl+O` 打开表内查找，在当前表中搜索内容
- **快速跳转命中行** — 按 `Q` 跳到上一条命中行，按 `E` 跳到下一条

### 3. 编辑表数据

按 `` ` ``（反引号键）进入编辑模式（需要输入当天日期作为密码，格式 YYYYMMDD）。

编辑模式做成了 Navicat 风格的网格体验，即使完全不碰鼠标也能改数据：

- **方向键导航** — ↑↓←→ 在单元格之间移动焦点框，跨到最后一行时会自动进入「新增行」区域
- **开始编辑** — 焦点格按 `Enter` / `F2`，或者直接键入任意字符（该字符会作为起始值）
- **矩形框选** — 鼠标按住拖一个矩形，或者 `Shift + ↑↓←→` 扩展选区
- **批量覆写** — 选中多格后，直接键入新值会把选区里所有格子改成同一个值
- **向下填充** — `Ctrl+D` 把选区顶行的值复制到下方其它行
- **清空选区** — `Delete` / `Backspace` 把选中的格子清空成空字符串
- **复制选区** — `Ctrl+C` 把矩形选区按 TSV 格式复制到剪贴板
- **粘贴 TSV** — `Ctrl+V` 按选区大小铺开粘贴；在 1×1 选区粘贴多行 TSV 时会自动从焦点格向右下铺开
- **新增行** — 工具栏 `+ 添加行`、表尾幽灵行单击、或者 `Ctrl+Enter`
- **删除行** — 勾选复选框后点「删除选中」，或者 `Ctrl+Delete` / `Ctrl+-` 删除焦点所在行（有选区时删除选区覆盖的所有行）

**📝 文本选项 (F4)** — 在编辑模式下按 `F4` 或点工具栏「📝 文本选项」按钮，表格下方会出现一个可拉伸的文本面板：

- 自动加载当前焦点单元格的内容到大 `<textarea>`，适合改长字段 / 多行 JSON
- 焦点换到别的格子时，若面板里还有未写回的草稿，会弹提示条，避免漏改
- 「重置」丢弃草稿；「写回」把草稿写回该单元格；「复制」把当前草稿拷到剪贴板
- 面板顶部边缘可以拖动来调整高度

**仍然可用的现有功能**：
- **行复选框** — 勾选后可批量删除、复制、批量改字段
- **双击单元格查看器** — 仅在**非编辑模式**下保留；编辑模式下双击 = 单击开始编辑

**保存** — 编辑完点「保存更改」，所有改动一次性提交。页面底部会显示改动统计（几条更新、几条新增、几条删除）。

### 4. 导出 Excel

- **导出当前表** — 快捷键 `Ctrl+E`，弹出保存对话框，选择位置后导出为 `.xlsx` 文件
- **批量导出** — 快捷键 `Ctrl+Shift+E`，弹出表选择窗口，勾选要导出的表，选择一个文件夹，每张表会生成一个独立的 `.xlsx` 文件

导出的 Excel 文件包含表头（加粗）和全部数据行。

### 5. 快速插入日期

在任意输入框中按 `F9`（默认），自动插入今天的日期，方便填写日期字段。

### 6. 表命令面板

按 `Ctrl+P` 打开命令面板，输入表名快速筛选并跳转到指定表，比用鼠标翻找更快。

### 7. 复制功能

搜索结果中的表名、字段名旁边有复制按钮（⎘），点击即可复制到剪贴板。

---

## 小鹰宠物

桌面上那只像素风小鹰不只是好看，它还有这些功能：

- **拖动** — 按住小鹰可以拖到桌面任意位置
- **锁定** — 右键菜单可以锁定位置，防止误拖
- **待机动画** — 5 分钟没操作后，小鹰会自动播放待机动画：
  - 浮浮呼吸 — 轻轻上下浮动
  - 打盹(zzz) — 睡觉
  - 左右张望 — 东看看西看看
  - 半透明潜行 — 变透明
- **搜索反馈** — 搜到结果时小鹰会有反应动画

右键小鹰弹出菜单：
- 打开搜索 — 打开搜索面板
- 设置 — 打开设置
- 退出程序 — 关闭软件

---

## 快捷键一览

### 全局快捷键（任何时候都能用）

| 快捷键 | 功能 | 可自定义 |
|--------|------|----------|
| `Ctrl+Shift+F` | 打开/关闭搜索面板 | 是 |
| `F9` | 在输入框插入今天日期 | 是 |

### 搜索面板快捷键

| 快捷键 | 功能 |
|--------|------|
| `Enter` | 开始搜索 |
| `/` | 选择搜索的表范围 |
| `Backspace`（搜索框为空时） | 移除最后一个已选的表 |

### 表数据窗口快捷键

| 快捷键 | 功能 | 可自定义 |
|--------|------|----------|
| `Tab` | 切换「只看命中」和「正常页面」 | 否 |
| `W` | 全屏/退出全屏 | 否 |
| `Q` | 跳到上一条命中行 | 否 |
| `E` | 跳到下一条命中行 | 否 |
| `` ` `` | 进入/退出编辑模式 | 否 |
| `Ctrl+O` | 表内查找 | 否 |
| `Ctrl+P` | 打开表命令面板 | 否 |
| `Ctrl+[` | 切换到上一个标签页 | 否 |
| `Ctrl+]` | 切换到下一个标签页 | 否 |
| `Ctrl+E` | 导出当前表 | 是 |
| `Ctrl+Shift+E` | 批量导出 | 是 |
| `Esc` | 关闭当前弹窗/面板 | 否 |

### 编辑模式快捷键

| 快捷键 | 功能 |
|--------|------|
| `↑ ↓ ← →` | 移动网格焦点框 |
| `Shift + ↑↓←→` | 扩展矩形选区 |
| `Enter` / `F2` | 进入单元格编辑 |
| `Esc`（焦点态） | 清除选区 |
| `Esc`（编辑中） | 取消编辑 |
| 可打印字符 | 进入编辑并将该字符作为起始值；选区 >1 格时直接批量覆写 |
| `Tab` / `Shift+Tab` | 编辑态左右跳格 |
| `Enter` / `Shift+Enter` | 编辑态上下跳格 |
| `Ctrl+D` | 向下填充（复制顶行到选区剩余行） |
| `Ctrl+C` | 复制矩形选区为 TSV |
| `Ctrl+V` | 粘贴 TSV 到选区（按选区大小铺开） |
| `Delete` / `Backspace` | 清空选区/焦点单元格 |
| `Ctrl+Enter` | 追加新行并把焦点移入 |
| `Ctrl+Delete` / `Ctrl+-` | 删除焦点所在行（或选区覆盖的所有行） |
| `F4` | 切换 📝 文本选项面板 |
| `Ctrl+S` | 保存所有更改 |

---

## 设置说明

右键小鹰 → 设置，或者在搜索面板中打开设置。

### 数据库连接

| 选项 | 说明 |
|------|------|
| 地址 | MySQL 服务器地址，比如 `localhost` 或 `192.168.1.100` |
| 端口 | 默认 `3306` |
| 用户名 | 数据库登录用户名 |
| 密码 | 数据库登录密码 |
| 数据库 | 要搜索的数据库名称 |

### 系统设置

| 选项 | 说明 | 默认值 |
|------|------|--------|
| 快捷键 | 全局唤起搜索面板 | `Ctrl+Shift+F` |
| 日期快捷键 | 在输入框插入今天日期 | `F9` |
| 导出快捷键 | 导出当前查看的表 | `Ctrl+E` |
| 批量导出快捷键 | 打开批量导出 | `Ctrl+Shift+E` |
| 小窗口默认视图 | 打开表时默认显示哪种视图 | 只看命中 |
| 开机自启 | 开机时自动启动软件 | 关闭 |
| 界面缩放 | 调整界面大小，范围 0.8 ~ 1.4 | 1.0 |

### 搜索设置

| 选项 | 说明 | 默认值 |
|------|------|--------|
| 排除表 | 不想搜索的表，支持正则表达式，多个用逗号隔开 | `^t_log_.*,^tmp_.*` |
| 单表超时 | 搜索每张表最多等几秒 | 10 秒 |
| 单表最大行数 | 每张表最多返回多少行结果 | 50 行 |

### 宠物设置

可以勾选开启或关闭每种待机动画，还能点预览按钮看看效果。

### 主题切换

提供四种配色方案：
- **Azure**（蓝色）— 默认主题
- **Midnight**（深灰）
- **Emerald**（绿色）
- **Violet**（紫色）

---

## 常见问题

**Q：连不上数据库怎么办？**
检查设置里的地址、端口、用户名、密码是否正确，确认 MySQL 服务已启动，并且允许远程连接。

**Q：搜索太慢怎么办？**
- 用 `/` 指定要搜索的表，不要全库搜
- 在设置里添加排除规则，跳过日志表等大表
- 选择「仅元数据」模式，不搜数据内容
- 适当降低「单表最大行数」

**Q：怎么修改快捷键？**
打开设置，在对应的快捷键输入框上点一下，然后按你想要的快捷键组合即可。按 `Backspace` 或 `Delete` 可以清空。

**Q：小鹰挡住了屏幕内容怎么办？**
拖动小鹰到不碍事的位置，或者右键锁定位置防止误拖。

**Q：导出的 Excel 文件在哪？**
单表导出时自己选保存位置；批量导出时选一个文件夹，每张表会在该文件夹下生成一个以表名命名的 `.xlsx` 文件。
